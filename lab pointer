#include<stdio.h>

int summary(int B[],int i)
{
	if(i > 5)
	{
		return 0 ;
	}
	return B[i] + summary(B, i+1) ;
}
int main()
{
	int B[5] = {1,2,3,4,5} ;
	int *ptr ;
	ptr = A ;
	printf("%d",sum(ptr,0));
}
