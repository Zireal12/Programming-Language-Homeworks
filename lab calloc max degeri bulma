#include<stdio.h>
#include<stdlib.h>


int main()
{
	int a , *ptr , max = 0 ;
	scanf("%d",&a);	
	ptr =(int*)calloc(a , sizeof(int));
	for(int i = 0;i < a;i++)
	{
		scanf("%d",ptr[i]);
		if(ptr[i] > max)
		{
			max = ptr[i] ;
		}
	}
	printf("%d",max);
		
	return 0;
}
